# ── core ──────────────────────────────────────────────────────────────────────
torch>=2.0
numpy>=1.23
pandas>=1.5

# ── eval / signal processing ──────────────────────────────────────────────────
scikit-learn>=1.1        # roc_auc_score used in eval_msl.py
scipy>=1.10              # savitzky-golay (future RW-2 impl)

# ── experiment tracking (optional; pipeline runs without it) ──────────────────
wandb>=0.16
python-dotenv>=1.0       # .env configuration

# ── testing ───────────────────────────────────────────────────────────────────
pytest>=7.0
