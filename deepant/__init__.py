"""Vendored DeepAnT forecasting pipeline.

Copied (and lightly de-coupled from TimeEval) from
``TimeEval-algorithms/deepant`` so that ``rwml-autocegar`` is self-contained.

Only structural changes were made (imports, packaging); the model, dataset,
training and detection logic are unchanged from the upstream DeepAnT.
"""
